name: "Security scans"

on: push

permissions: read-all

jobs:
  security:
    name: Security checks
    permissions:
      security-events: write
    uses: PossibleLlama/workflows/.github/workflows/golang-security.yaml@v0.1.9
    with:
      upload-sarif: true
